<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'TechCorp Solutions' %></title>
    
    <!-- Bootswatch Theme (Lux) for Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootswatch@5.3.3/dist/lux/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- Custom CSS -->
    <link rel="stylesheet" href="<%= basePath %>/css/style.css">
    
    <style>
        .navbar-brand {
            font-weight: bold;
            color: #007bff !important;
        }
        
        .hero-section {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 100px 0;
        }
        
        .feature-card {
            transition: transform 0.3s ease;
            border: none;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        
        .feature-card:hover {
            transform: translateY(-5px);
        }
        
        .footer {
            background-color: #343a40;
            color: white;
            padding: 40px 0;
            margin-top: 50px;
        }
        
        .alert-custom {
            border-radius: 10px;
        }
        
        .nav-link.active {
            font-weight: bold;
        }
        
        .table-responsive {
            border-radius: 10px;
            overflow: hidden;
        }
        
        .btn-custom {
            border-radius: 25px;
            padding: 10px 25px;
        }
        
        .card-custom {
            border-radius: 15px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm">
        <div class="container">
            <a class="navbar-brand" href="<%= basePath %>/">
                <i class="fas fa-code"></i> TechCorp Solutions
            </a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link <%= currentPath === '/' ? 'active' : '' %>" href="<%= basePath %>/">
                            <i class="fas fa-home"></i> Home
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= currentPath.startsWith('/database') ? 'active' : '' %>" href="<%= basePath %>/database">
                            <i class="fas fa-database"></i> Our Services
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= currentPath.startsWith('/contact') ? 'active' : '' %>" href="<%= basePath %>/contact">
                            <i class="fas fa-envelope"></i> Contact
                        </a>
                    </li>
                    
                    <% if (locals.isRegistered) { %>
                    <li class="nav-item">
                        <a class="nav-link <%= currentPath.startsWith('/messages') ? 'active' : '' %>" href="<%= basePath %>/messages">
                            <i class="fas fa-comments"></i> Messages
                        </a>
                    </li>
                    <% } %>
                    
                    <% if (locals.isAdmin) { %>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="adminDropdown" role="button" data-bs-toggle="dropdown">
                            <i class="fas fa-cog"></i> Admin
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item <%= currentPath.startsWith('/crud') ? 'active' : '' %>" href="<%= basePath %>/crud">
                                <i class="fas fa-edit"></i> Manage Products
                            </a></li>
                            <li><a class="dropdown-item <%= currentPath.startsWith('/messages') ? 'active' : '' %>" href="<%= basePath %>/messages">
                                <i class="fas fa-inbox"></i> View Messages
                            </a></li>
                        </ul>
                    </li>
                    <% } %>
                </ul>
                
                <ul class="navbar-nav">
                    <% if (locals.isAuth) { %>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown">
                            <i class="fas fa-user"></i> <%= locals.user.username %>
                            <% if (locals.isAdmin) { %>
                                <span class="badge bg-danger">Admin</span>
                            <% } else if (locals.isRegistered) { %>
                                <span class="badge bg-primary">Registered</span>
                            <% } %>
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="<%= basePath %>/logout">
                                <i class="fas fa-sign-out-alt"></i> Logout
                            </a></li>
                        </ul>
                    </li>
                    <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link" href="<%= basePath %>/login">
                            <i class="fas fa-sign-in-alt"></i> Login
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="<%= basePath %>/register">
                            <i class="fas fa-user-plus"></i> Register
                        </a>
                    </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main>
        <%- body %>
    </main>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <h5><i class="fas fa-code"></i> TechCorp Solutions</h5>
                    <p>Leading technology company providing innovative solutions for businesses worldwide.</p>
                    <div class="social-links">
                        <a href="#" class="text-white me-3"><i class="fab fa-facebook-f"></i></a>
                        <a href="#" class="text-white me-3"><i class="fab fa-twitter"></i></a>
                        <a href="#" class="text-white me-3"><i class="fab fa-linkedin-in"></i></a>
                        <a href="#" class="text-white me-3"><i class="fab fa-github"></i></a>
                    </div>
                </div>
                <div class="col-md-6">
                    <h5>Quick Links</h5>
                    <ul class="list-unstyled">
                        <li><a href="<%= basePath %>/" class="text-white-50">Home</a></li>
                        <li><a href="<%= basePath %>/database" class="text-white-50">Our Services</a></li>
                        <li><a href="<%= basePath %>/contact" class="text-white-50">Contact Us</a></li>
                        <% if (locals.isRegistered) { %>
                        <li><a href="/messages" class="text-white-50">Messages</a></li>
                        <% } %>
                    </ul>
                </div>
            </div>
            <hr class="my-4">
            <div class="row">
                <div class="col-md-12 text-center">
                    <p>&copy; <span id="year"></span> TechCorp Solutions. Web Programming II Homework Project.</p>
                </div>
            </div>
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Custom JS -->
    <script src="<%= basePath %>/js/main.js"></script>
        <script>
            document.getElementById('year').textContent = new Date().getFullYear();
        </script>
</body>
</html>