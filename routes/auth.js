const express = require('express');
const router = express.Router();

// This module handles additional authentication routes
// Main auth routes are in start.js for compatibility

module.exports = router;