# Production Environment Variables for TechCorp Solutions
# Copy this file to .env and configure for your production environment

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=techcorp
# IMPORTANT: Use a strong password (min 16 characters recommended)
# Password should include: uppercase, lowercase, numbers, and special characters
# Generate strong password: openssl rand -base64 24
DB_PASS=YOUR_SECURE_DATABASE_PASSWORD_HERE
DB_NAME=company_db

# Application Configuration
PORT=3000
NODE_ENV=production

# Session Configuration
# IMPORTANT: Generate a strong random secret for production
# This secret is used to sign session cookies
# Generate with: openssl rand -base64 32
# Or: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
SESSION_SECRET=YOUR_SECURE_SESSION_SECRET_HERE

# Security Notes:
# 1. Never commit this file with real credentials to version control
# 2. Use strong, unique passwords for production (minimum 16 characters)
# 3. Passwords should contain uppercase, lowercase, numbers, and symbols
# 4. Restrict database user permissions to only what's needed
# 5. Consider using environment-specific secrets management (e.g., HashiCorp Vault)
# 6. Rotate secrets regularly (every 90 days recommended)
# 7. Use different credentials for development, staging, and production
